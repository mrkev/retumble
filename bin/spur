#!/usr/bin/env node

var program = require('commander');

program
  .command('init',  'Initializes a new blank theme')
  .command('run',   'Runs a test server to test a theme')
  .command('ready', 'Generates bundle file to publish theme')

program.parse(process.argv);
